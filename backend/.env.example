DATABASE_URL=postgresql://postgres:password@localhost:5432/razorresolve
SECRET_KEY=razorresolve-dev-secret-key-change-in-production
ENVIRONMENT=development
LOG_LEVEL=info
# Optional: OpenAI key — if absent the app uses deterministic mock AI
OPENAI_API_KEY=
